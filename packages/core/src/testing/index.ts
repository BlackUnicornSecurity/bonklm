/**
 * Test-Only Helpers — Public Barrel
 * =================================
 * Exports under the `@blackunicorn/bonklm/testing` subpath.
 *
 * Only for use in unit tests. Importing anything from this subpath in
 * production code is a code-review red flag and will be enforced by a
 * future lint rule.
 */
export { noOpValidator } from './no-op-validator.js';
